// scripts/lib/zod-schemas.ts — re-export from canonical location.
// Canonical source of truth lives at lib/data/zod-schemas.ts so the UI
// (Plan #2) and the generation scripts (Plan #1) share one schema.
export * from "@/lib/data/zod-schemas";
